# Portfolio Website

This is a modern personal portfolio website built with HTML and CSS.

## Preview locally

Open `index.html` in your browser, or run:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000`.

## Customize

Update the content in `index.html` and `styles.css` with your own name, bio, projects, and contact details.
